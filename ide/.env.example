# Anthropic API Key (optional)
# Get your API key from: https://console.anthropic.com/settings/keys
#
# If set, enables live Claude API usage metrics (tokens remaining, request limits)
# If not set, the IDE will show tier limits but not real-time usage
#
# Format: sk-ant-api03-...
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# -----------------------------------------------------------------------------
# Keycloak (Red Hat) — optional enterprise SSO
# -----------------------------------------------------------------------------
# When all of these are set, the IDE requires login via Keycloak (OAuth 2.0 / OIDC).
# When unset, the IDE runs without authentication.
#
# KEYCLOAK_URL=https://auth.example.com
# KEYCLOAK_REALM=squad
# KEYCLOAK_CLIENT_ID=squad-ide
# KEYCLOAK_CLIENT_SECRET=your-client-secret
# KEYCLOAK_REDIRECT_URI=https://ide.example.com/auth/keycloak/callback
# KEYCLOAK_POST_LOGOUT_URI=https://ide.example.com
