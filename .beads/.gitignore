# .beads/.gitignore
# This directory contains user-specific task data that should NOT be committed
# Each user creates their own tasks when they run 'bd init' on their projects

# =============================================================================
# IGNORE ALL USER DATA (each user creates their own)
# =============================================================================

# SQLite databases (local cache, rebuilt from JSONL)
*.db
*.db?*
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Task data - user-specific, don't commit
issues.jsonl
deletions.jsonl
completions.json
task-images.json
task-images.json.bak
review-rules.json

# Signals - user session history, don't commit
signals/

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# Database backups
*.backup-*
*.bak
*.gz

# =============================================================================
# KEEP THESE (templates/config)
# =============================================================================

# Config template (shows available options)
!config.yaml

# This gitignore file
!.gitignore

# Metadata (just structure info, not user data)
!metadata.json
