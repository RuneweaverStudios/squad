#!/bin/bash
# jat-memory - Agent memory indexer CLI
# Reads .jat/memory/*.md, chunks, embeds, stores in SQLite for search.
#
# Usage:
#   jat-memory index [--force] [--project path] [--skip-embeddings] [--verbose]
#   jat-memory status [--project path] [--json]
#   jat-memory providers
#   jat-memory --help

SOURCE="${BASH_SOURCE[0]}"
while [ -L "$SOURCE" ]; do
  DIR="$(cd "$(dirname "$SOURCE")" && pwd)"
  SOURCE="$(readlink "$SOURCE")"
  [[ "$SOURCE" != /* ]] && SOURCE="$DIR/$SOURCE"
done
SCRIPT_DIR="$(cd "$(dirname "$SOURCE")" && pwd)"
exec bash "$SCRIPT_DIR/run.sh" "$@"
