# .beads/.gitignore
# =============================================================================
# ⚠️  THIS GITIGNORE IS SPECIAL FOR THE JAT REPOSITORY (open source)
# =============================================================================
#
# This ignores ALL task data so JAT contributors don't commit their personal tasks.
#
# YOUR OWN PROJECTS use a DIFFERENT default gitignore that SYNCS tasks via git!
# When you run 'bd init' on your project, you get the standard gitignore with:
#   !*.jsonl  ← Keeps issues.jsonl for team collaboration
#
# Don't copy this file to your projects - let 'bd init' create the correct one.
# =============================================================================

# =============================================================================
# IGNORE ALL USER DATA (each user creates their own)
# =============================================================================

# SQLite databases (local cache, rebuilt from JSONL)
*.db
*.db?*
*.db-journal
*.db-wal
*.db-shm

# Daemon runtime files
daemon.lock
daemon.log
daemon.pid
bd.sock

# Legacy database files
db.sqlite
bd.db

# Task data - user-specific, don't commit
issues.jsonl
deletions.jsonl
completions.json
task-images.json
task-images.json.bak
review-rules.json

# Signals - user session history, don't commit
signals/

# Merge artifacts (temporary files from 3-way merge)
beads.base.jsonl
beads.base.meta.json
beads.left.jsonl
beads.left.meta.json
beads.right.jsonl
beads.right.meta.json

# Database backups
*.backup-*
*.bak
*.gz

# =============================================================================
# KEEP THESE (templates/config)
# =============================================================================

# Config template (shows available options)
!config.yaml

# This gitignore file
!.gitignore

# Metadata (just structure info, not user data)
!metadata.json
